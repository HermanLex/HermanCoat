# Stripe (Dashboard → Developers → API keys)
STRIPE_SECRET_KEY=sk_test_...

# Public site URL for Stripe redirects (production). Optional in dev (uses request host).
# NEXT_PUBLIC_APP_URL=https://hermancoat.com
